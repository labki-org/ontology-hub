export function ModuleDetail({
  entityKey,
  draftId: _draftId,
  isEditing: _isEditing,
}: {
  entityKey: string
  draftId?: string
  isEditing: boolean
}) {
  return <div>Module detail for {entityKey} (placeholder)</div>
}
