export function SubobjectDetail({
  entityKey,
  draftId,
  isEditing,
}: {
  entityKey: string
  draftId?: string
  isEditing: boolean
}) {
  return <div>Subobject detail for {entityKey} (placeholder)</div>
}
