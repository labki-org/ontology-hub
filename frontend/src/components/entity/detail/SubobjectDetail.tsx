export function SubobjectDetail({
  entityKey,
  draftId: _draftId,
  isEditing: _isEditing,
}: {
  entityKey: string
  draftId?: string
  isEditing: boolean
}) {
  return <div>Subobject detail for {entityKey} (placeholder)</div>
}
