export function BundleDetail({
  entityKey,
  draftId: _draftId,
  isEditing: _isEditing,
}: {
  entityKey: string
  draftId?: string
  isEditing: boolean
}) {
  return <div>Bundle detail for {entityKey} (placeholder)</div>
}
