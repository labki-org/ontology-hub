export function PropertyDetail({
  entityKey,
  draftId: _draftId,
  isEditing: _isEditing,
}: {
  entityKey: string
  draftId?: string
  isEditing: boolean
}) {
  return <div>Property detail for {entityKey} (placeholder)</div>
}
