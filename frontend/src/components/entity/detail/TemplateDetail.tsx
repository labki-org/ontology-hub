export function TemplateDetail({
  entityKey,
  draftId: _draftId,
  isEditing: _isEditing,
}: {
  entityKey: string
  draftId?: string
  isEditing: boolean
}) {
  return <div>Template detail for {entityKey} (placeholder)</div>
}
