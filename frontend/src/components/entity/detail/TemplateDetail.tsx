export function TemplateDetail({
  entityKey,
  draftId,
  isEditing,
}: {
  entityKey: string
  draftId?: string
  isEditing: boolean
}) {
  return <div>Template detail for {entityKey} (placeholder)</div>
}
